<div class="toot-form">
    <%= form_for toot, url: create_toot_path do |f| %>
      <%= f.label :message %><br />
      <%= f.text_area :message, autofocus: true, cols: 60, rows: 5 %>
      <br>
      <%= f.submit "Toot It!", class: "btn btn-primary" %>
    <% end %>
  </div>
